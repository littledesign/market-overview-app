export const environment = {
  production: false,
  /** Uses relative /api so Angular dev proxy forwards to backend (avoids CORS) */
  apiBaseUrl: '/api',
  supabaseUrl: 'https://your-project.supabase.co',
  supabaseAnonKey: 'your-anon-key-here',
};
